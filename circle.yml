dependencies:
  pre:
    - pip install -r requirements.txt

test:
  override:
    - nosetests --with-coverage --cover-package=datascope_tools --with-xunit --xunit-file=tests/nosetests.xml
    - pep8 datascope_tools/

machine:
  environment:
    CIRCLE_TEST_REPORTS: tests
